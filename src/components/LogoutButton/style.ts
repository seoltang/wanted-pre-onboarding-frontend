import styled from 'styled-components';
import { RoundedBorderButton } from '@styles/common';

export const LogoutBtn = styled(RoundedBorderButton)`
  align-self: flex-end;
  width: max-content;
  font-size: 14px;
`;
